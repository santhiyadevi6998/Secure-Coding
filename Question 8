Securing File Uploads (ASP.NET)
Question:
Fill in the blank to validate the file type of a file upload in ASP.NET by checking the MIME type.

csharp
if (file.ContentType == "__________")  // Fill in the blank
{
    // Process the file
}
